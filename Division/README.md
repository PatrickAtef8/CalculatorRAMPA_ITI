# Divide Module
To create the static library write the following command in the Division directory.
```bash
cmake -S . -B build -G Ninja
cd ./build
ninja
``` 
You will find the static library *libDivide.a* inside the build directory.